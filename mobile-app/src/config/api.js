// Adresse de base de l'API backend.
//
// IMPORTANT : "localhost" fonctionne uniquement dans le simulateur iOS (il partage
// le réseau du Mac hôte). Sur Android Studio Emulator, un appareil physique, ou Expo Go,
// "localhost" pointe vers l'appareil lui-même, pas vers ta machine de développement.
// Remplace-le par l'adresse IP locale de ton PC sur le réseau Wi-Fi
// (trouvable avec `ipconfig` sous Windows, cherche "Adresse IPv4"), par exemple :
//   export const API_URL = 'http://192.168.1.42:5000/api';
// Ton téléphone/émulateur et ton PC doivent être sur le même réseau Wi-Fi.
export const API_URL = 'http://192.168.1.65:5000/api';
